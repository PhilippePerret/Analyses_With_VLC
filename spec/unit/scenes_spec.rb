# encoding: UTF-8
# frozen_string_literal: true
=begin
	Module pour tester les scènes
=end
require_relative '_required'

describe 'TEST DES SCÈNES' do
	before :all do
		params = Preparator.init_with(film: :default)
	end  

  it 'une scène peut être créée au point temps voulu' do
  	
  end


end
